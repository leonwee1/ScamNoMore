node_modules/
dist/
*.log

# Never commit secrets
.env
.env.local

# Local media used for debugging transcription. Personal recordings and large
# video files should never end up in the repo.
samples/
*.mp4
*.mov
*.m4a
*.mp3
*.wav

# Local SQLite state (including its WAL companion files) must never be deployed
# from Git or treated as a source fixture.
data/
*.sqlite
*.sqlite-wal
*.sqlite-shm
